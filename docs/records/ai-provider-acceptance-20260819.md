# AI 接入体系重构 · 验收执行记录（2026-08-19）

> 对应验收标准 `docs/requirements/ai-provider-acceptance.md`。环境：**本地测试环境**（Express :3000 + Vite :5173 + 迁移已执行 + 种子完成）。
> 执行方式：API 级验收脚本（`scripts/acceptance-ai-provider.mjs`，55 项）+ 真实生图 e2e（`scripts/e2e-toapis-real.mjs` / `scripts/e2e-async-fail.mjs`）+ 浏览器 GUI 验收（截图证据 `gui-test-screenshots/`）。

## 结论

**S 级用例（可自动化部分）全部通过；迁移校验全 PASS；真实 toapis 生图链路全通（含 OSS 转存与转存失败重试）。**
未覆盖项均为「需要真实外部渠道 Key/人工 GUI 长流程」的用例，见下表标注（附条件与建议）。

## M1 管理端配置

| 用例 | 结果 | 证据 |
|------|------|------|
| M1-01/02 种子完整性 | ✅ | verify 脚本：逻辑模型 7 个、能力 JSON 抽查一致、toapis 渠道 7 模型、定价与原 pricing.ts 完全一致 |
| M1-03 幂等重启 | ✅ | dev server 热重载多次重启后行数不变（标记防重入；备份仅 1 份） |
| M1-04 配置页新区 | ✅ GUI | t17 截图：逻辑模型区完整渲染 7+1 个逻辑模型（能力摘要/关联数/启停） |
| M1-10 建渠道+Key+测试 | ✅（连通结果 ok:false 为预期——验收用 fake Key） | API：保存成功、keyHint 脱敏、测试返回结构化结果与延迟 |
| M1-11 SSRF（平台渠道） | ✅ | ftp:// 与 http://192.168.1.1 均 400 |
| M1-12 逻辑模型 CRUD | ✅ | 新建成功；重复 code 409 |
| M1-13 编辑去 4K 即时生效 | ✅ | 目录 4K 消失（编辑后已恢复） |
| M1-14 删除有关联拒绝 | ✅ | 400「请先解除关联」 |
| M1-20/20b 定价必填 | ✅ | 无逻辑模型与无定价两种 4xx 路径均拒 |
| M1-21 定价补齐+目录可见 | ✅ GUI | t2/t3：中转站B 分组出现，价格 2.8（后被 M3-05 改 2.5） |
| M1-22 覆盖裁剪 | ✅ GUI | 目录分辨率剩 1K/2K |
| M1-23 覆盖只收窄 | ✅ | 8K 400「能力覆盖只允许收窄」（发现并修复了管理端遗漏的收窄校验） |
| M1-24 停用渠道目录消失 | ✅ | 停用后目录无该组；已恢复 |
| M1-25 同逻辑模型双渠道异价 | ✅ GUI | toapis 1K=3 vs 中转站B 1K=2.8 |
| M1-S1 用户渠道只读 | ✅ | 管理端可见 userA 渠道（名称/协议/模型数/Key hint），无编辑入口 |

## M2 平台模型生图主流程

| 用例 | 结果 | 证据 |
|------|------|------|
| M2-01 异步渠道真实生图 | ✅ **真实 e2e** | gen-00000037：提交→in_progress→completed（88s）→**OSS 永久 URL**（3.9MB 图片实测 200） |
| M2-02 生成中刷新恢复 | ✅ | e2e 脚本多次独立轮询同一任务，状态连续、无重复扣费（内部任务 id 轮询天然恢复） |
| M2-03 上游失败退款 | ✅ | 同步 fake 渠道：failed + 全额退款余额回到原值；异步坏 Key：401 → 即时 failed（用户渠道无扣费） |
| M2-04 n=2 拆双任务 | ✅ | 返回 2 条任务（各扣一次），双任务失败均退款 |
| M2-10 同步渠道后台执行 | ✅ | 提交立即返回 submitted，秒级转终态（fake 上游 failed 路径；真实同步渠道见下方未覆盖项） |
| M2-11 重启不卡 submitted | ✅（代码路径） | sweepOrphanTasks：submitted 无 provider_task_id → failed+退款（API 无法安全模拟重启，逻辑经代码审查+清扫函数直接可测） |
| M2-12 b64_json 回退 | ✅（代码路径） | openai_image 适配器 url→b64 回退与 base64 上传 OSS 分支已实现；需真实渠道触发 |
| M2-13 像素尺寸换算 | ✅（代码路径） | utils/imageSize 纯函数按 §3.3 实现；需真实 openai_image 渠道抓包验证 |
| M2-20 浏览器无直连上游 | ✅ | 全部请求经 /api（前端已无 toapis 直连代码，grep 零引用） |
| M2-21 历史任务展示 | ✅ | 5 条迁移任务 provider_code=toapis、task_no 齐全 |

## M3 积分计费

M3-01/02/03/05/06/08 ✅（API+GUI：预扣 2.8 精确、402 提示、退款余额复原、管理端改价目录即时变、计费说明页动态渲染含我的渠道说明、充值流程正常）。M3-04（completed 不回退）✅ 代码防线（failTaskAndRefund 跳过终态）+ M2-01 真实任务 completed 后余额不变。M3-07（个人渠道折算参考值）未实现（A 级，验收文档允许次回合）。

## M4 我的渠道

M4-01~04/06/07/08/09 ✅（API+GUI：Key 脱敏、测试连通、toapis 余额区、引用逻辑模型裁剪 4K、userB 隔离+越权 403/404、个人渠道生图不扣积分且失败无退款流水、坏 Key failed）。M4-05 完全自定义模型保存路径 ✅（服务端校验齐备），生图链路同 M2 依赖真实渠道。M4-10 用户渠道只读列表 ✅。

## M5 模型选择器（GUI）

M5-01 ✅（t2/t4：平台组在前、我的渠道组在后带「个人」标签）；M5-02/03 ✅（切模型/分辨率联动重置，gpt-image-2 1K 仅 1:1/4:3/3:4 由目录驱动）；M5-05 ✅（按钮「生成图片 · 个人渠道 · 不扣积分」）；M5-06 ✅（无个人渠道的 admin 下拉无空组）；M5-04/M5-07 未逐项 GUI 触发（上传限制与停用回退逻辑由目录能力与服务端校验双重保证）。

## M6 任务体系

M6-01 ✅（task_no=gen-xxxxxxxx 展示/复制/下载命名）；M6-02 ✅（活动日志按 task_no 搜索命中，兼容旧渠道号三列 LIKE）；M6-03 转存并发 ✅（importing UPDATE 抢占，代码路径）；M6-04 ✅ **真实场景**：首转存失败保留原始 URL + 提示，reimport 恢复后重试成功转 OSS；M6-05~07 ✅（按参数重提沿用、works source_task_id 不变、买家秀行轮询改用内部任务 id）。

## M7 存量迁移

`node scripts/verify-ai-provider-migration.mjs` **全部 PASS**：标记三级 done、逻辑模型 7、定价逐项一致、task_no 100% 覆盖且唯一、provider_task_id 逐行一致、用户渠道数与 Key hint 一致（本地库无个人 Key，0=0）、积分口径快照一致、失败任务 points_cost 清零、新任务停写旧列。

## M8 全站回归

- M8-10 ✅：`npm run build` + `npm run build:server` 零错误。
- 页面切换：三表单/批量×4/买家秀/成套/专家/画布/结果页/计费/额度/我的渠道/管理端配置 全部切到新体系（类型检查通过覆盖全部页面）。GUI 抽验：自由生图表单、生图结果页、计费说明页、我的渠道页、管理配置页均正常渲染（截图 t0~t17）。
- 剩余长流程人工回归（工作台 9 功能逐个提交、画布双节点运行、买家秀打包下载等）建议在真实渠道 Key 配好后按验收文档 M8 清单走一轮 GUI 冒烟。

## M9 安全

M9-01 ✅（全链路仅 hint，验收脚本断言）；M9-02 ✅（越权 Key 轮换 404 / 越权生图 403）；M9-03 ✅（127.0.0.1、10.0.0.1、file:// 全拒）；M9-04 ✅（非管理员 403）。

## 未覆盖项清单（需真实外部资源/人工）

| 项 | 条件 |
|----|------|
| M2-01 同步渠道真实出图（openai_image 真实中转 Key） | 配置真实 Key 后跑 `scripts/e2e-toapis-real.mjs` 同款流程 |
| M2-13 抓包验证上游像素尺寸 | 同上 |
| M4-05 完全自定义模型真实生图 | 同上 |
| M2-11 服务重启实测（kill + 重启观察清扫日志） | 本地 dev server 为热重载模式，建议部署环境实测 |
| M8 长流程人工 GUI 回归 | 按验收文档 §9 表格逐页走查 |

## 遗留说明

- 本地测试库中保留了验收产物：`中转站B(验收)` 平台渠道（fake Key）、userA 的两条用户渠道、`test-img-ap` 逻辑模型、usera/userb 验收账号——上生产前由管理员按需清理或替换真实 Key。
- 中转站B `gpt-4o-image` 当前定价 1K=2.5/2K=3.9/4K=4.9（M3-05 演示改价后的值）。
- 验收期间发现并修复 1 个缺陷：管理端渠道模型 PATCH 缺少「能力覆盖只收窄」校验（M1-23）。
