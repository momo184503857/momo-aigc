node_modules/
dist/
server/dist/
# 数据库文件（含数据，不入库）
server/data/
# Node-RED 实例运行时产物（userdir/.config.json 等，不入库）
server/nodered/userdir/
*.log
*.tsbuildinfo
vite.config.js
vite.config.d.ts
# 环境变量（含密钥，不入库）
.env
.env.local
.env.*.local

# ZCode 临时文件
.zcode/

# AI 助手本地指令（仅本机使用，不入库）
AGENTS.md
