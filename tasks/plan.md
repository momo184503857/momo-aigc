# 渠道自动路由实施计划

1. 扩展数据库：逻辑模型统一售价、渠道模型成本价、任务逻辑模型、路由尝试记录，并迁移现有价格。
2. 重构模型目录与管理 API：普通用户只获得逻辑模型和售价；管理员分别维护售价和成本价。
3. 重构生图编排：按能力与成本产生候选，只用渠道首个 Key，失败逐渠道切换，保存尝试记录并保持单次扣退费。
4. 收口前端：选择器只显示逻辑模型，所有生图入口提交 `logicalModelId`，统一显示逻辑模型售价。
5. 更新管理界面与验收脚本，执行类型检查、构建和迁移校验。

风险控制：异步任务只在明确失败终态时跨渠道；状态不明的网络错误保持当前渠道；迁移保留旧字段并兼容旧 `channelModelId` 请求。

---

# 逻辑模型渠道拖拽优先路由计划（2026-09-11）

1. 扩展 `ai_models.route_priority` 基线与幂等迁移，历史行保持确定性顺序。
2. 扩展逻辑模型管理 API，返回关联渠道路由信息并原子保存完整顺序。
3. 将生成路由改为 `route_priority ASC, ai_models.id ASC`，成本仅保留为资格与诊断字段。
4. 在逻辑模型页增加编辑弹窗、拖拽列表、状态信息、显式保存和键盘移动。
5. 执行迁移、API/路由定向验证、类型检查与构建，保护已有脏工作区。

---

# 逻辑模型渠道路由开关实施计划（2026-09-11）

1. 为渠道模型增加逻辑模型级 `route_enabled` 状态，并兼容迁移历史数据为启用。
2. 扩展逻辑模型路由配置 API，原子保存拖拽顺序与参与路由开关。
3. 生图候选查询排除已关闭路由的渠道模型，同时保留既有失败切换和退款语义。
4. 在逻辑模型编辑弹窗增加“参与路由”开关、关闭状态展示和显式保存。
5. 完成接口、数据库、浏览器交互、类型与构建验证。
